# Особые правила наборов

Страницы Figma определяют состав экспорта. Для `colors_<название_набора>.json` загрузчик ищет одноимённый модуль `<название_набора>.mjs` рядом с собой; исходник хранится как `<название_набора>.mts`. Подчёркивания в имени сохраняются. Если такого файла нет, действуют стандартные правила из [layout.mts](../layout.mts).

Каждый модуль экспортирует объект, проверенный через `satisfies Rule` из [contract.mts](../contract.mts).

| Поле | Назначение |
|---|---|
| `section` | Обязательное регулярное выражение для полного имени секции |
| `frame` | Необязательное выражение для имени фрейма; по умолчанию строчные буквы, цифры и дефисы |
| `defaultOnly` | `true` или функция от имён секции и фрейма, если допускается только состояние `default` |
| `literal` | Сохранение имени секции целиком без выделения динамического режима |

Файл содержит только отличия набора. Формулы цветов, ключей, `figma`, `web`, `alias`, порядок полей, сортировка и устаревание общие и не переопределяются.

Новая стандартная страница не требует модуля. Удаление модуля не отключает оставшуюся страницу: она перейдёт на стандартные правила. Отключение выполняется удалением или переименованием страницы.

[Как добавить или отключить набор](../../docs/ADD_OR_REMOVE_PALETTE.md) · [Общий алгоритм](../../docs/FIGMA_FORMAT.md) · [Карточки наборов](../../docs/palettes/README.md)
